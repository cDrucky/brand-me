import React from "react"
import { Card, CardHeader, CardBody } from "reactstrap"

const Baseball = () => {
	return (
		<Card>
			<CardHeader>Baseball</CardHeader>
			<CardBody>Get some baseball API or build a web scraper to pull some data so I can showcase it here!</CardBody>
		</Card>
	)
}

export default Baseball
